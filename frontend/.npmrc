# pnpm ≤10 读取本文件（pnpm 11+ 改为读 pnpm-workspace.yaml，两处需保持一致）。
#
# 依赖与缓存全部落在仓库目录内，不写 $HOME（见 CLAUDE.md「本地环境」）：
#   frontend/.pnpm-store    内容寻址存储（默认在 ~/.local/share/pnpm/store）
#   frontend/.pnpm-cache    元数据缓存（默认在 ~/.cache/pnpm）
#
# 历史坑：本目录原有 .pnpmrc（内容与本文件相同），但 pnpm 8 与 pnpm 12 都不读它，
# 于是 shamefully-hoist 一直未生效——因此改用 .npmrc。
store-dir=.pnpm-store
cache-dir=.pnpm-cache
shamefully-hoist=true
